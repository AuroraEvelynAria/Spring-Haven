# 春日庭院（Spring Haven）· 主要功能

> [English version · 英文版](PROJECT.md)

这份文档说明春日庭院是什么、**哪些真的做出来了**、**哪些设计好了但还没做**、**哪些还只是设想**。
每一节都做了标注，不夸大任何一条：

**✅ 已实现 ｜ 🔧 已设计 / 规划中 ｜ 💭 远期设想**

---

## 一、本地 AI 生活模拟

角色会在这个既定的区域内自由生活，并且有自己的日程，会时不时用「手机」来和用户进行交流，
打语音通话或者发短信，偶尔会用手机拍照，拍下 3D 虚拟空间里的景象。
角色偶尔会把手机拿倒就是了 awa

> ✅ **已经做进去的**：角色会**主动发消息**（离线消息由角色自己的 LLM 与稳定人格提示词生成，
> 不用本地模板冒充），也会**在 3D 场景里拍照然后主动发给你**；日程按小时排布。
>
> 💭 **还在设想的**：手机这个载体本身、语音通话、短信。

## 二、自带的地形编辑器

帮助用户可以自定义住宅所在地区的地理地形，以及住宅内部的装潢，
精细到墙上的开关位置，还有每件家具的朝向。

> 🔧 **规划中**（Phase 3，基于 GridMap）。
>
> 「精细到墙上开关位置、每件家具朝向」是我的设计意图，还没实现。

## 三、2D 交互 —— 最不消耗性能的互动方式

用户可以启用非静态角色展示模式：用 **Live2D** 和角色面对面聊天，
但此时程序不会保留 3D 空间的渲染，而是保存并关闭，节省性能开销。
如果用户有摄像头的话，角色的眼睛会盯着用户的脸，并且会做出适当的动作。

> ✅ **已经做进去的**：**分层立绘适配器**（眨眼、视线跟随、呼吸、TTS 音量驱动的口型、
> 7 种表情、生活状态驱动的表情变化）；2D 舞台与 3D 场景是**分开的场景**，所以 2D 下不渲染 3D 空间。
>
> 🔧 **未实现**：Live2D 本体。
>
> 💭 **还在设想的**：摄像头识别人脸。

关于「让 AI LLM 来控制 Live2D 好像很困难吧」—— 我的做法是**不让 LLM 碰表现层**：
立绘适配器只暴露一组最小能力面：

```text
set_role(role_id)
set_expression(expression)
set_thinking(active)
set_body_state(state)
speak(text, duration)
set_tts_mouth_level(level)
```

LLM 只输出受控的语义信号，适配器负责表现。正式接 Live2D 时，只需新增一个实现同样接口的
适配器，**不改 Core 协议**，分层立绘也仍然是可发布的回退方案。

**但这条路我没验证过 —— 如果有人做过 Live2D 这边，我很想知道你们的做法。**

## 四、ASR 与 TTS

以上功能涉及最基础的 ASR 和 TTS。
ASR 用 Whisper 就够了。TTS 我希望能找到一种**能带来自然音调变化和情绪**的方案。

> ✅ **已经接好的**：ASR 走 OpenAI 兼容 `/audio/transcriptions` 或 Open-LLM-VTuber `/asr`；
> TTS 已经有 **GPT-SoVITS、Voicebox、CozyVoice**，以及任何 OpenAI 兼容语音接口。
>
> 💭 **还在找的**：「自然音调变化 + 情绪」这一条我还没满意，欢迎推荐。

## 五、3D 展示模式（支持 VR）

用户得以进入角色所生活的住宅里，和她们一起生活，包括烹饪、看电视、看书等。
根据开发计划，日后会加入完整小镇模型，让你可以带角色出门玩，
可以有电影院、游乐园和海滩，也有其他的 AI NPC ——
他们是游戏引擎支撑的角色，默认与 AI LLM 没有关系。

> ✅ **已经做进去的**：3D 探索场景已有原型（自主导航 + 障碍规避）；
> **烹饪、读书、看节目已经是「两人共同活动」**，在生活实验室里可以触发。
>
> 🔧 **规划中**：完整小镇。
>
> 💭 **还在设想的**：电影院、游乐园、海滩、VR，以及「用户亲自走进住宅」的形态。
>
> 「NPC 由引擎驱动、默认不接大模型」是我的设计意图 —— 这条同时**压住了 token 成本**。

## 六、UGC —— 类似创意工坊的社区

让每个人都可以上传自己做好的角色和住宅（包括地理地形），
互相分享和使用别人做好的内容。
我相信这会**极大延续项目的生命周期**。

> 🔧 **规划中**（Phase 3，Steam 创意工坊）。

## 七、生命系统

角色有自己的生命状态。

**两种性别都共有的**：健康、体力、饥饿、口渴、困意值、好感度、心情、压力值

**女性独有的经期系统会影响角色的情绪变化。**

引擎负责记录数值变化，**按阶段变化来告知 LLM 模型**，让角色作出不同的行为。
（避免轮询带来极高的 token 消耗并显著降低缓存命中率。）

**不含 NSFW 内容，也不支持 NSFW 内容。**

> ✅ **已经做进去的、并且比这里列的更多**：一共 **11 项** ——
> 健康、体力、饥饿、口渴、**清醒**（代码里是 `awake`，就是「困意值」的正面表达）、
> **膀胱充盈**、好感度、心情、压力、**内膜容受性**、**着床倾向**。
> 「经期系统」在代码里建模为后两项，比「影响情绪」更具体。
>
> ✅ 「按阶段变化告知 LLM、避免轮询」这条**写得很准**，它对应 ADR-002 数值卫生 + 滞后阈值：
> **原始生理浮点数一个都不进上下文**，先分桶成定性描述再给模型。
>
> ✅ **不含 NSFW，而且是应用级强制** —— 固定动作白名单，没有任何设置能打开。

## 八、心织 Heartloom 记忆系统

我把它单独做成了一个模块，叫「心织」（Heartloom）—— 把相处织成记忆的意思。

它不是「向量库 + 把聊天记录塞回去」那种做法，
我当成一个**有生命周期的系统**在设计的：

- 记忆像人一样：常提起的很清晰，很久没提的会慢慢变成模糊的印象，**但不会丢**
- 时间锚的是【**世界时间**】，不是现实时间 —— 所以角色离线那段时间发生的事，和她的记忆是对得上的
- **数值不进上下文**：生命状态里的数字全部先转成定性描述再给 LLM，原始数值一个都不进
  —— 不然模型会被数字带着跑，人设会飘
- 长期的东西会压成**摘要和里程碑**，而不是一直堆原文
- 全部存**本地 SQLite**，不上云

它和生命系统的关系是：**生命系统管「此刻」，心织管「过去」。**
状态变化被记下来，下次开口的时候，以记忆的形式回来。

> ✅ **全部已经实现。**
>
> 补充几条我另外做的（不影响上面的表述）：
>
> - 中英文**本地关键词索引**（中文用 2–4 字 n-gram，**不依赖云端 embedding**）
> - 召回结果写入**动态** `<heartloom_memory_context>`，**不污染稳定 System 前缀** ——
>   前缀缓存命中实测 **76%+**
> - 记忆之间有**可解释的关系图**，每条边返回成因，用 IDF 压低全库高频词，
>   并对节点数、边数、单节点度数设上限，保证长期运行后仍可交互
>
> ⚠️ 一条诚实的说明：「锚世界时间」的设计已定，但**周键到世界周的迁移还没做完**（issue #23）。

## 九、项目灵感来源

- **斯坦福小镇 generative_agents** — https://github.com/joonspk-research/generative_agents
- **Neuro-Sama**（未开源）
- **Atri** — 虚构角色
- **昔涟桌宠 Agent** — https://github.com/Playa-0v0/Cyrene-Agent
  （原仓库出问题了，访问可以看我的 fork：https://github.com/AuroraEvelynAria/Study-from-Cyrene-Agent）
- **LifeBook** — https://github.com/Anson-Trio/LifeBook
- **Open-LLM-VTuber** — https://github.com/Open-LLM-VTuber/Open-LLM-VTuber

---

## 许可

- **源代码**：MIT
- **素材（美术、模型、UI、音乐）**：CC BY-NC 4.0

> ⚠️ 部分占位素材来自第三方（MMD 等），将在商业发行前替换。
