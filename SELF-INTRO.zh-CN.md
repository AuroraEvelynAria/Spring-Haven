# 自我介绍 —— AuroraEvelynAria

> [English version · 英文版](SELF-INTRO.md)

写给我在 [AIRI Discussion #33](https://github.com/moeru-ai/airi/discussions/33) 的留言。

大家好，我是 **Jin Xi**（GitHub [`AuroraEvelynAria`](https://github.com/AuroraEvelynAria)），
一个人做业余开发。我一直在做一个自己托管的 AI 陪伴项目，叫 **春日庭院（Spring Haven）**。
想在这里介绍自己，是因为其中有一块正好和 AIRI 还在做的东西重叠：**长期记忆**。

## 我在做什么

**春日庭院** 是一个自己托管、自己拥有的 AI 陪伴项目：两个角色拥有自己的生命状态、
自己的日程、自己的记忆，住在一栋你可以走进去的小房子里。

| 部分 | 技术栈 | 规模 |
|---|---|---|
| 客户端 | Godot 4.7 / GDScript | **118 个 `.gd` · 32,964 行** |
| 后端 | Python 3.11+（aiohttp + SQLite/WAL） | **17 个模块 · 11,293 行** |
| 测试 | `unittest` | **20 个文件 · 5,492 行 · 183 项全绿** |

对应这个帖子里列出的方向，这几块是真的在跑的：

- **语音识别（ASR）** —— 走 OpenAI 兼容的 `/audio/transcriptions`（Whisper）或
  Open-LLM-VTuber 的 `/asr`。
- **语音合成（TTS）** —— 支持 **GPT-SoVITS**，以及任何 OpenAI 兼容语音接口。
  「自然音调变化 + 情绪」这一条我还没满意，如果你找到了更好的方案，我真的很想听。
- **表现层** —— 分层 2D 立绘适配器：眨眼、视线跟随、呼吸、TTS 音量驱动的口型、
  7 种表情、生活状态驱动的表情变化。**Live2D 本体没做** —— 适配器只暴露一组最小接口
  （`set_role / set_expression / set_thinking / set_body_state / speak / set_tts_mouth_level`），
  就是为了让接 Live2D 时只需新增一个适配器，而不用改协议。**但这条路我没验证过。**

## 可能对 AIRI 有用的部分：心织（Heartloom）

AIRI 的 **Memory Alaya** 还在 WIP。我遇到了同一个问题，然后从另一个角度把它单独做成了
一个模块，叫 **心织（Heartloom）** —— 把相处织成记忆的意思。

它**不是**「向量库 + 把聊天记录塞回去」那种做法，我把它当成一个**有生命周期的系统**在设计：

- **记忆像人一样会老。** 常提起的很清晰，很久没提的会慢慢变成模糊的印象 —— **但不会丢。**
  遗忘只降低召回权重，从不删行。
- **时间锚的是【世界时间】，不是现实时间。** 角色离线那段时间发生的事，和她的记忆是对得上的。
- **原始数值一个都不进上下文。** 生命状态里的每个浮点数都先分桶成定性描述，
  不然模型会被数字带着跑、人设会飘。
- **长期的东西会压成摘要和里程碑**，而不是一直堆原文。
- **全部存本地 SQLite**，不上云。
- **本地关键词索引，不依赖云端 embedding。** 中文用 2–4 字 n-gram，所以完全离线也能召回。
- **召回结果写入*动态*上下文块**，绝不污染稳定的 System 前缀 —— 前缀缓存命中实测 **76%+**。
- **记忆之间有可解释的关系图。** 每条边都会返回成因，用 IDF 压低全库高频词，
  并对节点数、边数、单节点度数设上限，保证长期运行后仍然可交互。

对外提供 `GET /memory/status` 和 `GET /memory/graph`。

它和生命系统的关系是：**生命系统管「此刻」，心织管「过去」。** 状态变化被记下来，
下次开口的时候，以记忆的形式回来。

## 我还没做的

我宁愿把话说清楚，也不想夸大：

- **Live2D** —— 适配器接口设计好了，没实现。
- **VR、完整小镇、UGC** —— 设计好了，没做。
- **记忆时间锚定里的「周键 → 世界周」迁移**（issue #23）—— 设计已定，还没做完。
- **摄像头识别人脸** —— 还是设想。

完整的功能清单，每一节都标了 `✅ 已实现 / 🔧 已设计 / 💭 远期设想`，
在 [`PROJECT.md`](PROJECT.zh-CN.md) 里。那份文档是特意写成不夸大任何一条的 ——
包括**不把已经做出来的东西写成还没做**。

## 我觉得自己真能帮上忙的地方

我的技术栈是 GDScript + Python，不是 Vue/TypeScript，所以我不会假装自己能直接进 AIRI 前端。
我觉得自己确实有用的地方是：

- **记忆层 / Memory Alaya** —— 这是我花时间最多的一块，也是最想和大家交流的一块。
- **ASR / TTS 适配** —— 这两条我都对着真实服务接通过，包括 GPT-SoVITS。
- **后端测试覆盖** —— 183 项测试是我能带过来的习惯。

如果这些里有能用的，我很乐意出力。如果有人做过 Live2D 这边，我真的很想知道你们是怎么做的。

**Looking forward to contribute.**
