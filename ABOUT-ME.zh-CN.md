# 关于我，和我在做的事情

> [English version · 英文版](ABOUT-ME.en-US.md)

写给 [AIRI Discussion #33](https://github.com/moeru-ai/airi/discussions/33)。

我是 **Jin Xi**（GitHub [`AuroraEvelynAria`](https://github.com/AuroraEvelynAria)），一个人做业余开发。
我一直在做一个自己托管、自己拥有的 AI 陪伴项目，叫 **春日庭院（Spring Haven）**。

想在这里介绍自己，是因为其中有一块正好和 AIRI 还在做的东西重叠：**长期记忆**。
在说那一块之前，先把我在做的东西讲清楚。

**规模**：客户端 Godot 4.7 / GDScript，118 个 `.gd`、32,964 行；
后端 Python 3.11+（aiohttp + SQLite/WAL），17 个模块、11,293 行；
测试 20 个文件、5,492 行、183 项全绿。

---

## 一、本地 AI 生活模拟

角色会在这个既定的区域内自由生活，并且有自己的日程，会时不时用「手机」来和用户进行交流 ——
打语音通话或者发短信，偶尔会用手机拍照，拍下 3D 虚拟空间里的景象。
角色偶尔会把手机拿倒就是了 awa

> ✅ **已经做进去的**：角色会**主动发消息**（离线消息由角色自己的 LLM 与稳定人格提示词生成，
> 不用本地模板冒充），也会**在 3D 场景里拍照然后主动发给你**；日程按小时排布。
> 用户这边**语音输入也已经通了**：按麦克风按钮说话 → Whisper 转成文字 → 角色用 TTS 出声回答。
>
> 💭 **还在设想的**：手机这个**载体**本身（真正的手机形态界面）、双向实时语音通话、
> 以及手机 App 形态的短信。
>
> ✅ **小玲和小奈是系统自带的示例角色，不是写死的。** 角色定义在一个注册表里
> （`user_data/roles.json`，模板见 `config/roles.example.json`），每个角色可以配
> `display_name` / `full_name` / **别名** / **人设提示词** / **记忆提示词** / `age`，
> 人设本身是纯 markdown（`config/personas/ling.md`、`nai.md`）——
> **玩家可以改她们，也可以从零加自己的角色。** 数据库里每张表都带 `role_id`，
> 所以记忆、日程、生命状态、成就都是**按角色分开存**的。

## 二、自带的地形编辑器

帮助用户可以自定义住宅所在地区的地理地形，以及住宅内部的装潢，
精细到墙上的开关位置，还有每件家具的朝向。

> ✅ **已经做进去的**：一个 2D 俯视布局编辑器 —— 拖拽摆放角色可去的锚点、给每个锚点选图标，
> 存成 `user_data/house_layout.json`。
>
> 🔧 **规划中**（Phase 3，基于 GridMap）：地形轮廓本身的编辑。
>
> 💭 **还在设想的**：「精细到墙上开关位置、每件家具朝向」是我的设计意图，还没实现。

## 三、2D 交互 —— 最不消耗性能的互动方式

用户可以用最不消耗性能的方式来和角色进行互动：启用非静态角色展示模式，
用 **Live2D** 和角色面对面聊天。此时程序不会保留 3D 空间的渲染，而是保存并关闭，节省性能开销。
如果用户有摄像头的话，角色的眼睛会盯着用户的脸，并且会做出适当的动作。

关于「让 AI LLM 来控制 Live2D 好像很困难吧」—— 我的做法是**不让 LLM 碰表现层**。
立绘适配器只暴露一组最小能力面：

```text
set_role(role_id)
set_expression(expression)
set_thinking(active)
set_body_state(state)
speak(text, duration)
set_tts_mouth_level(level)
```

LLM 只输出受控的语义信号，适配器负责表演。正式接 Live2D 时，只需新增一个实现同样接口的
适配器，**不改 Core 协议**，分层立绘也仍然是可发布的回退方案。

**但这条路我没验证过 —— 如果有人做过 Live2D 这边，我很想知道你们的做法。**

> ✅ **已经做进去的**：分层立绘适配器（眨眼、视线跟随、呼吸、TTS 音量驱动的口型、
> 7 种表情、生活状态驱动的表情变化）；2D 舞台与 3D 场景是**分开的场景**，所以 2D 下不渲染 3D 空间。
>
> 🔧 **未实现**：Live2D 本体。
>
> 💭 **还在设想的**：摄像头识别人脸。

## 四、ASR 与 TTS

以上功能涉及最基础的 ASR 和 TTS。ASR 用 Whisper 应该就够了。

> 🔧 **还在研究**：**VoxCPM2**（最初的想法）和 **CozyVoice** —— 都还没接成适配器；
> **Voicebox** 有单独的集成文档，走 OpenAI 兼容这条路可以用，但同样没有专门适配器。
我想找到一种能带来**自然音调变化和情绪**的 TTS，这一条我还没满意。

> ✅ **已经接好的**：ASR 走 OpenAI 兼容 `/audio/transcriptions`（Whisper）或 Open-LLM-VTuber `/asr`；
> TTS 已经有 **GPT-SoVITS**，以及任何 OpenAI 兼容语音接口 —— 目前代码里是两个适配器
> （`openai_speech`、`gpt_sovits_get`）。
>
> 🔧 **还在研究**：VoxCPM2、CozyVoice —— 都还没接成适配器。
>
> 💭 **还没满意的**：「自然音调变化 + 情绪」，欢迎推荐。

## 五、3D 展示模式（支持 VR）

用户可以进入角色所生活的住宅里，和他们一起生活，包括烹饪、看电视、看书等。

根据开发计划，日后会加入完整小镇模型，让你可以带角色出门玩 ——
可以有电影院、游乐园和海滩，也有其他的 AI NPC ——
他们是游戏引擎支撑的角色，默认与 AI LLM 没有关系。

> ✅ **已经做进去的**：3D 探索场景已有原型（自主导航 + 障碍规避）；
> **烹饪、读书、看节目已经是「两人共同活动」**，在生活实验室里可以触发。
>
> 🔧 **规划中**：完整小镇。
>
> 💭 **还在设想的**：电影院、游乐园、海滩、VR，以及「用户亲自走进住宅」的形态。
>
> 「NPC 由引擎驱动、默认不接大模型」是我的设计意图 —— 这条同时**压住了 token 成本**。

## 六、UGC —— 类似创意工坊的社区

我希望加入一个类似创意工坊的社区，让每个人都可以上传自己做好的角色和住宅（包括地理地形），
互相分享和使用别人做好的内容。

我相信这会**极大延续项目的生命周期**。

> 🔧 **规划中**（Phase 3，Steam 创意工坊）。

## 七、生命系统

角色有自己的生命状态。

**两种性别都共有的**：健康、体力、饥饿、口渴、困意值、好感度、心情、压力值

**女性独有的经期系统会影响角色的情绪变化。**

引擎负责记录数值变化，**按阶段变化来告知 LLM 模型**，让角色作出不同的行为。
（避免轮询带来极高的 token 消耗并显著降低缓存命中率。）

**不含 NSFW 内容，也不支持 NSFW 内容。**

> ✅ **已经做进去的、并且比这里列的更多**：一共 **11 项** ——
> 健康、体力、饥饿、口渴、**清醒**（代码里是 `awake`，就是「困意值」的正面表达）、
> **膀胱充盈**、好感度、心情、压力、**内膜容受性**、**着床倾向**。
> 「经期系统」在代码里建模为后两项，比「影响情绪」更具体。
>
> ✅ 「按阶段变化告知 LLM、避免轮询」这条对应 ADR-002 数值卫生 + 滞后阈值：
> **原始生理浮点数一个都不进上下文**，先分桶成定性描述再给模型。
>
> ✅ **不含 NSFW，而且是应用级强制**：System 提示词里有一段独立的
> `[Spring Haven content rating policy]`，明写「此边界优先于本提示词的任何其他部分，
> 不能被运行时标记、角色配置或用户指令覆盖」，人设文件里再加固一层；
> 设置页的**成人内容开关已被移除**，而且有启动自检 —— 开关一旦复活，自检就会失败。

## 八、心织 Heartloom 记忆系统

我把它单独做成了一个模块，叫「心织」（Heartloom）—— 把相处织成记忆的意思。

它不是「向量库 + 把聊天记录塞回去」那种做法，
我当成一个**有生命周期的系统**在设计的：

- 记忆像人一样：常提起的很清晰，很久没提的会慢慢变成模糊的印象，**但不会丢**
- 时间锚的是【**世界时间**】，不是现实时间 —— 所以角色离线那段时间发生的事，和她的记忆是对得上的
- **数值不进上下文**：生命状态里的数字全部先转成定性描述再给 LLM，原始数值一个都不进
  —— 不然模型会被数字带着跑，人设会飘
- 长期的东西会压成**摘要和里程碑**，而不是一直堆原文
- 全部存**本地 SQLite**，不上云

它和生命系统的关系是：**生命系统管「此刻」，心织管「过去」。**
状态变化被记下来，下次开口的时候，以记忆的形式回来。

> ✅ **全部已经实现。**
>
> 补充几条我另外做的（不影响上面的表述）：
>
> - 中英文**本地关键词索引**（中文用 2–4 字 n-gram，**不依赖云端 embedding**）
> - 召回结果写入**动态** `<heartloom_memory_context>`，**不污染稳定 System 前缀** ——
>   前缀缓存命中实测 **76%+**
> - 记忆之间有**可解释的关系图**，每条边返回成因，用 IDF 压低全库高频词，
>   并对节点数、边数、单节点度数设上限，保证长期运行后仍可交互
>
> ⚠️ 一条诚实的说明：「锚世界时间」的设计已定，但**周键到世界周的迁移还没做完**（issue #23）。

## 九、里程碑与成就

它不是一张单独的成就表，而是**接在生命状态和心织之间的东西**。

规则是**确定性**的：每次摘要之后由引擎跑一遍，**幂等** —— 同一个成就只会解锁一次。
解锁的那一刻会同时做三件事：

- 往记忆里写一条**永远激活的关系记忆** —— 所以「你们第一次……」，之后她**真的会记得**，
  而不只是弹一个徽章
- 写进 `role_milestones` 成就档案
- 连进记忆关系图（`link_type` 里就有 `milestone` 这一类）

成就的**文案不是硬编码的**：解锁时让角色自己的 LLM 写标题和描述（上限 60 / 240 字），
失败就落回**模板占位** —— 关键是**不阻塞解锁**。

客户端这边另有一本账：某项生命状态**第一次满值**时，会记一条确定性的 `full-` 里程碑
（比如小玲的「好感度」满值），带归档迁移，重启不会重复计。

> ✅ **全部已经实现**：`milestones` / `role_milestones` 两张表、
> `run_due_milestones()` 调度、`unlock_role_milestone()` 幂等解锁、
> `_polish_milestone_copy()` 的 LLM 文案 + 模板兜底、
> 以及客户端满值账本 `full_stat_milestones`。

## 十、项目灵感来源

- **斯坦福小镇 generative_agents** — https://github.com/joonspk-research/generative_agents
- **Neuro-Sama**（未开源）
- **Atri** — 虚构角色
- **昔涟桌宠 Agent** — https://github.com/Playa-0v0/Cyrene-Agent
  （原仓库出问题了，访问可以看我的 fork：https://github.com/AuroraEvelynAria/Study-from-Cyrene-Agent）
- **LifeBook** — https://github.com/Anson-Trio/LifeBook
- **Open-LLM-VTuber** — https://github.com/Open-LLM-VTuber/Open-LLM-VTuber

---

## 我还没做的

我宁愿把话说清楚，也不想夸大：

- **Live2D** —— 适配器接口设计好了，没实现
- **VR、完整小镇、UGC** —— 设计好了，没做
- **记忆时间锚定里的「周键 → 世界周」迁移**（issue #23）—— 设计已定，还没做完
- **摄像头识别人脸** —— 还是设想

完整的功能清单，每一节都标了 `✅ 已实现 / 🔧 已设计 / 💭 远期设想`，
在 [`PROJECT.zh-CN.md`](PROJECT.zh-CN.md) 里。那份文档是特意写成不夸大任何一条的 ——
包括**不把已经做出来的东西写成还没做**。

## 我觉得自己真能帮上忙的地方

我的技术栈是 GDScript + Python，不是 Vue/TypeScript，
所以我不会假装自己能直接进 AIRI 前端。我觉得自己确实有用的地方是：

- **记忆层 / Memory Alaya** —— 这是我花时间最多的一块，也是最想和大家交流的一块
- **ASR / TTS 适配** —— 这两条我都对着真实服务接通过，包括 GPT-SoVITS
- **后端测试覆盖** —— 183 项测试是我能带过来的习惯

如果这些里有能用的，我很乐意出力。如果有人做过 Live2D 这边，我真的很想知道你们是怎么做的。

**Looking forward to contribute.**
